{
  "name": "@darktriad/preload",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.cjs",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --sourcemap --out-dir dist --out-extension cjs=.cjs",
    "watch": "tsup src/index.ts --format cjs --sourcemap --out-dir dist --watch --out-extension cjs=.cjs"
  },
  "dependencies": {
    "zod": "3.23.8"
  },
  "devDependencies": {
    "tsup": "8.1.0"
  }
}
